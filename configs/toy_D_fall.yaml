# Arm D + freeze=all: signal-triggered growth with the same protection as C_fall.
arm: signal
freeze: all
model: {n_experts: 4}
detector: {window: 50, z_threshold: 4.0, min_history: 20}
cooldown_steps: 300
